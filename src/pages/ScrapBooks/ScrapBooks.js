import clsx from "clsx";
import styles from './ScrapBooks.module.scss'

function ScrapBooks() {
    return ( 
        <h1 className={clsx(styles.scrapbooks)}>
            scrapbooks
        </h1>
     );
}

export default ScrapBooks;